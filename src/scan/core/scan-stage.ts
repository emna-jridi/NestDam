export enum ScanStage {
  INIT = 'init',
  PERMISSIONS = 'permissions',
  HEURISTICS = 'heuristics',
  TRACKERS = 'trackers',
  ML = 'ml',
  SAAT = 'saat',
  NETWORK = 'network',
  FINALIZE = 'finalize',
}
